#include <iostream>
#include <cmath>

double v, sin, a, g, h, t;

int main()
{
    double first = v * sin * a;
    double second = 1 / 2 * (g * h * t);
    double y = first * t - second;

    return 0;
}