#include <iostream>
#include <cmath>

double a, x, u, v;

int main()
{
    // v^2 missing
    double equation1 = pow(v, 2);
    double equation2 = pow(u, 2) + 2 * a * x;
    equation1 = equation2;

    return 0;
}